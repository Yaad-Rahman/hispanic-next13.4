export type SliderSettings = {
  dots: boolean;
  dotsClass: string;
  infinite: boolean;
  speed: number;
  slidesToShow: number;
  slidesToScroll: number;
  prevArrow: JSX.Element | null; // Set to null
  nextArrow: JSX.Element | null; // Set to null
};
